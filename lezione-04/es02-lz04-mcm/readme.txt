CONSEGNA:

Scrivere un programma che calcoli il minimo comune multiplo di due numeri. 

- Scrivere una libreria con una funzione per il calcolo dell'MCM e una libreria per l'output, e 
- mettere tutto insieme in un file index.php (per le librerie usare require) che preveda due valori di default 
- oppure valori specificati tramite un file di configurazione (per la configurazione usare include).

-permetti in input x numeri e con un costrutto printa il risultato per ogni combo


formula mcm:

    (18)$a = 2 * 3^2
    (24)$b = 2^3 * 3

    $mcm = 2^3 * 3^2